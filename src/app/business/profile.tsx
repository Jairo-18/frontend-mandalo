import { BusinessProfileScreen } from '@/components/business/business-profile-screen';

/** "Mi negocio" (rol NEGO): resumen del negocio + editar (BusinessFormModal). */
export default function BusinessProfileRoute() {
  return <BusinessProfileScreen />;
}
