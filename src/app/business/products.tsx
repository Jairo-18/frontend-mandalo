import { ProductCrudScreen } from '@/components/business/product-crud-screen';

/** Productos del negocio autenticado (CRUD completo con fotos). */
export default function BusinessProductsScreen() {
  return <ProductCrudScreen />;
}
