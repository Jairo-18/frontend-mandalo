import { ChangePasswordScreen } from '@/components/profile/change-password-screen';

/** Cambiar contraseña del CLIENTE (pantalla compartida en components/profile). */
export default function ClientChangePasswordRoute() {
  return <ChangePasswordScreen />;
}
