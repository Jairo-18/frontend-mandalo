import { BulkInviteScreen } from '@/components/admin/bulk-invite-screen';

/** "Alta masiva": el admin sube/escribe correos y crea cuentas en bloque. */
export default function BulkInviteRoute() {
  return <BulkInviteScreen />;
}
