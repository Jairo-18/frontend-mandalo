import { AppSettingsScreen } from '@/components/admin/app-settings-screen';

/** "Aplicación": el admin edita la paleta de marca de toda la app (§50). */
export default function AppSettingsRoute() {
  return <AppSettingsScreen />;
}
