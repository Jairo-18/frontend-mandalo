import { ResendDocumentsScreen } from '@/components/profile/resend-documents-screen';

/** Reenviar documentos del REPARTIDOR (corregir un rechazo o renovar uno vencido). */
export default function DeliveryResendDocumentsRoute() {
  return <ResendDocumentsScreen />;
}
