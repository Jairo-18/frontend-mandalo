import { ChangePasswordScreen } from '@/components/profile/change-password-screen';

/** Cambiar contraseña del REPARTIDOR (pantalla compartida en components/profile). */
export default function DeliveryChangePasswordRoute() {
  return <ChangePasswordScreen />;
}
